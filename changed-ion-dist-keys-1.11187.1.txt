ChatCodeExecEnabledHint	OLD=Allow Claude to run code in a local sandbox in Chat conversations. The sandbox can read only the files attached to the conversation and has no network access.	NEW=Allow Claude to run code in a local sandbox in Chat conversations. The sandbox can read only the files attached to the conversation and has no network access. Off by default; without it Claude can't analyze spreadsheets or other files that require computation in Chat.
